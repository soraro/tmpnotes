apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tmpnotes.fullname" . }}-test-connection"
  labels:
    {{- include "tmpnotes.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tmpnotes.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
