apiVersion: v1
kind: Service
metadata:
  name: {{ include "tmpnotes.fullname" . }}
  labels:
    {{- include "tmpnotes.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 5000
      protocol: TCP
      name: http
  selector:
    {{- include "tmpnotes.selectorLabels" . | nindent 4 }}
